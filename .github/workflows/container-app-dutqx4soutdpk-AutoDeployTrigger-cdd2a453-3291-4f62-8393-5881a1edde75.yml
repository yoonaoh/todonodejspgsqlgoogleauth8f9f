name: Trigger auto deployment for container-app-dutqx4soutdpk

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-dutqx4soutdpk-AutoDeployTrigger-cdd2a453-3291-4f62-8393-5881a1edde75.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPDUTQX4SOUTDPK_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPDUTQX4SOUTDPK_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPDUTQX4SOUTDPK_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPDUTQX4SOUTDPK_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPDUTQX4SOUTDPK_REGISTRY_PASSWORD }}
          containerAppName: container-app-dutqx4soutdpk
          resourceGroup: todonodejspgsqlgoogleauth8f9f9cd1
          imageToBuild: default/container-app-dutqx4soutdpk:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


